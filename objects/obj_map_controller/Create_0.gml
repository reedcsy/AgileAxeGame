selected_index = 0;

// Count all buttons that are children of the parent object
// We subtract 1 because index starts at 0 (e.g., 5 buttons = max index 4)
max_index = instance_number(obj_map_button_parent) - 1;